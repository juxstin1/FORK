# ============================================
# FORK Environment Variables
# Copy this file to .env and fill in your keys.
# One-time setup — never touch it again.
#
#   cp .env.example .env
#
# .env is gitignored. Your keys stay local.
# ============================================

# --- Supabase ---
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# --- OpenRouter ---
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxx
OPENROUTER_SITE_URL=http://localhost
OPENROUTER_APP_NAME=FORK MCP Server

# --- Docker Safety ---
# SAFE mode (default, unset): only read-only docker subcommands are allowed.
# POWER mode: set to I_UNDERSTAND to allow mutating docker subcommands.
# In SAFE mode, docker commands time out after 60s. In POWER mode, timeout is 300s.
FORK_DOCKER_POWER_MODE=
